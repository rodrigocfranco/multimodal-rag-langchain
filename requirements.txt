# Unstructured - para extração de dados de PDFs
unstructured[all-docs]==0.16.9
unstructured-inference==0.8.1
pillow==11.0.0
lxml==5.3.0

# OpenCV (headless para ambientes sem GUI)
opencv-python-headless==4.10.0.84

# Vector store e embeddings - LATEST VERSIONS
chromadb>=1.3.0,<2.0.0
langchain-chroma>=1.0.0,<2.0.0
tiktoken>=0.8.0,<1.0.0

# LangChain 1.x - LATEST STABLE (October 2025)
# Python 3.10+ required (dropped 3.9 support)
langchain>=1.0.2,<2.0.0
langchain-community>=0.4.1,<1.0.0
langchain-core>=1.0.1,<2.0.0
langchain-openai>=1.0.1,<2.0.0
langchain-groq>=1.0.0,<2.0.0
langchain-text-splitters>=0.3.3,<1.0.0

# Cohere (para reranking - melhora precisão em 30-40%)
langchain-cohere>=0.4.6,<1.0.0
cohere>=5.12.0,<6.0.0

# BM25 for Hybrid Search (keyword-based retrieval)
rank-bm25==0.2.2

# Metadata Enrichment (P0-3)
keybert==0.8.5
sentence-transformers==3.3.1
scikit-learn==1.6.0

# API REST
flask==3.1.0
flask-cors==5.0.0

# Utilitários
python-dotenv==1.0.1
