# Unstructured - para extração de dados de PDFs
unstructured[all-docs]==0.18.15
unstructured-inference==0.8.1
pillow==11.3.0
lxml==5.4.0

# OpenCV (headless para ambientes sem GUI)
opencv-python-headless==4.12.0.88

# Vector store e embeddings
# ChromaDB 0.5.x com langchain-chroma 0.2.x (compatível com langchain-core 0.3.x)
chromadb>=0.5.23,<0.6.0
langchain-chroma>=0.2.3,<0.3.0
tiktoken>=0.8.0,<1.0.0

# LangChain - VERSÕES CONGELADAS (evita resolution-too-deep)
langchain==0.3.16
langchain-community==0.4.1
langchain-core==0.3.32
langchain-openai==0.3.35
langchain-groq==0.3.8
langchain-text-splitters==0.3.3

# Cohere (para reranking - melhora precisão em 30-40%)
langchain-cohere==0.4.6
cohere==5.20.0

# BM25 for Hybrid Search (keyword-based retrieval)
rank-bm25==0.2.2

# Metadata Enrichment (P0-3)
keybert==0.9.0
sentence-transformers==3.4.1
scikit-learn==1.7.2

# API REST
flask==3.1.0
flask-cors==5.0.0

# Utilitários
python-dotenv==1.2.1
